# ruff: noqa: F821
# AST fixture — never executed. Parsed by the quality engine only.


def test_compute_total() -> None:
    # Calls the function but makes zero assertions — 0% contract coverage expected.
    compute_total([1, 2, 3])
